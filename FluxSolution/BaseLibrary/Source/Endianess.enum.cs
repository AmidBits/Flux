namespace Flux
{
  public enum Endianess
  {
    /// <summary>This is the MSB to LSB order (e.g. Motorola).</summary>
    BigEndian,
    /// <summary>This is the LSB to MSB order (e.g. Intel).</summary>
    LittleEndian
  }
}
