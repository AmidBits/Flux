namespace Flux
{
  public static partial class XtensionsSpan
  {
    /// <summary>Reverses the Span in-place.</summary>
    //public static void Reverse<T>(this System.Span<T> source)
    //{
    //  var sourceMaxIndex = source.Length / 2;

    //  for (int left = 0, right = source.Length - 1; left <= sourceMaxIndex; left++, right--)
    //  {
    //    var tmp = source[left];
    //    source[left] = source[right];
    //    source[right] = tmp;
    //  }
    //}
  }
}
