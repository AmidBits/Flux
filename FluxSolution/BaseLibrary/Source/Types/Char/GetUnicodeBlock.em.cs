namespace Flux
{
  public static partial class UnicodeEx
  {
    ///// <summary>Returns the Unicode block associated with in the specified UTF32 code point.</summary>
    //public static UnicodeBlock GetUnicodeBlock(this char source)
    //  => System.Enum.GetValues<UnicodeBlock>().Single(ub => source >= ub.GetMinUtf32() && source <= ub.GetMaxUtf32());
    //{
    //  foreach (var block in System.Enum.GetValues<UnicodeBlock>())
    //    if (source >= block.GetMinUtf32() && source <= block.GetMaxUtf32())
    //      return block;

    //  throw new System.ArgumentOutOfRangeException(nameof(source));
    //}
  }
}
